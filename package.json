{
  "name": "radon-mcp",
  "version": "0.1.0",
  "description": "MCP server for Radon IDE integration",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "build:watch": "node scripts/build.js --watch",
    "start": "node dist/index.js",
    "dev": "yarn build && yarn start",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.3"
  },
  "author": "noma4i",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
